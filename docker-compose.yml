services:
  api:
    restart: unless-stopped
    container_name: api
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    env_file:
      - ./apps/api/.env
    healthcheck:
      test: ["executable", "arg"]
      interval: 1m30s
      timeout: 30s
      retries: 5
      start_period: 30s
    networks:
      - sundate_network

  docs:
    restart: unless-stopped
    container_name: docs
    build:
      context: .
      dockerfile: apps/docs/Dockerfile
    env_file:
      - ./apps/docs/.env
    networks:
      - sundate_network

  nginx:
    image: nginx:alpine
    container_name: nginx
    restart: unless-stopped
    ports:
      - '${NGINX_PORT:-80}:80'
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      docs:
        condition: service_started
      api:
        condition: service_started
    networks:
      - sundate_network

networks:
  sundate_network:
    # driver: bridge
    # define as external to use the same network as the other services
    external: true
