name: 🚀 Deploy as a Staging Environment | Dellbuntu

on:
  push:
    branches:
      - staging-api

permissions:
  actions: write
  contents: read

env:
  ORIGIN_SERVER_NAME: 'justdemo.work'
  PROJECT_NAME: 'sundate'
  IMAGE_NAME_API: 'api'
  IMAGE_NAME_DOCS: 'docs'
  # No need to deploy mobile app, just build it

jobs:
  deploy-api:
    name: Deploy API Application
    runs-on: [self-hosted, dellbuntu]
    env:
      NGINX_PORT: 88
      BASE_API_URL: '/api'
      MONGO_URI: ${{ secrets.MONGO_URI }}
      JWT_SECRET: ${{ secrets.JWT_SECRET }}
      JWT_REFRESH_SECRET: ${{ secrets.JWT_REFRESH_SECRET }}
      ENABLE_PUSH_NOTIFICATIONS: ${{ secrets.ENABLE_PUSH_NOTIFICATIONS }}
      REDIS_URL: ${{ secrets.REDIS_URL }}
      REDIS_TLS_ENABLED: ${{ secrets.REDIS_TLS_ENABLED }}
      # The other environment variables are needless, can be default values
    steps:
      - uses: actions/checkout@v4

      - name: Create api .env from GitHub Secrets
        run: |
          cat <<EOF > apps/api/.env
          MONGO_URI=${{ env.MONGO_URI }}
          BASE_API_URL=${{ env.BASE_API_URL }}
          ENABLE_PUSH_NOTIFICATIONS=${{ env.ENABLE_PUSH_NOTIFICATIONS }}
          JWT_SECRET=${{ env.JWT_SECRET }}
          JWT_REFRESH_SECRET=${{ env.JWT_REFRESH_SECRET }}
          REDIS_URL=${{ env.REDIS_URL }}
          REDIS_TLS_ENABLED=${{ env.REDIS_TLS_ENABLED }}
          EOF

      - name: Create nginx .env
        run: |
          cat <<EOF > nginx/.env
          NGINX_PORT=${{ env.NGINX_PORT }}
          NGINX_DOMAIN=${{ env.PROJECT_NAME }}.${{ env.ORIGIN_SERVER_NAME }}
          EOF

      - name: Build API Application
        run: docker-compose -f docker-compose.api.yml up --build -d
